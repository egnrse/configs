# env.shrc
#
# a custom config file for bash/zsh (extends .bashrc/.zshrc)
# only sets variables if they are empty (might happen if connecting over eg. ssh/waypipe)
# (https://github.com/egnrse/configs)
# (by egnrse)

if [ -z "$EDITOR" ]; then
	#if command -v nvim >/dev/null 2>&1; then
	if [ -n "$(command -v nvim)" ]; then
		export EDITOR=nvim				# your prefered editor (eg. vim, nano)
	else
		export EDITOR=vim				# your prefered editor (eg. vim, nano)
	fi
fi
